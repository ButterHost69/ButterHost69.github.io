---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('posts', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const groups: Record<string, typeof posts> = {};
for (const post of posts) {
  const group = post.id.split('/')[0];
  if (!groups[group]) groups[group] = [];
  groups[group].push(post);
}

const groupDisplayNames: Record<string, string> = {
  'machine-learning': 'Machine-Learning',
  'networking': 'Networking',
  'random-stuff': 'Random Stuff',
};

const groupOrder = ['machine-learning', 'networking', 'random-stuff'];
const sortedGroups = Object.keys(groups).sort((a, b) => {
  const idxA = groupOrder.indexOf(a);
  const idxB = groupOrder.indexOf(b);
  if (idxA === -1 && idxB === -1) return a.localeCompare(b);
  if (idxA === -1) return 1;
  if (idxB === -1) return -1;
  return idxA - idxB;
});
---

<BaseLayout title="Blogs" description="All writing organized by topic.">
  <aside class="sidebar">
    <div class="sidebar-content">
      <a href="/" class="sidebar-logo">Palash S Patel</a>

      <div class="separator"></div>
      <nav class="sidebar-nav">
        <a href="/home">/home</a>
        <a href="/blogs">/blogs</a>
        <a href="/projects">/projects</a>
        <a href="/about">/about</a>
        <a href="/resume">/resume</a>
      </nav>
    </div>
  </aside>

  <main class="main-content">
    <h1 class="page-title">Blogs</h1>

    <div class="tree-container">
      {sortedGroups.map((group, groupIndex) => {
        const groupPosts = groups[group];
        const isLastGroup = groupIndex === sortedGroups.length - 1;
        const groupPrefix = isLastGroup ? '└─ ' : '├─ ';
        return (
          <div class="tree-group">
            <div class="tree-group-header">
              <span class="tree-prefix">{groupPrefix}</span>
              <span class="tree-group-name">{groupDisplayNames[group] || group}</span>
            </div>
            <div class="tree-group-children">
              {groupPosts.map((post, postIndex) => {
                const isLastPost = postIndex === groupPosts.length - 1;
                const postPrefix = isLastGroup ? (isLastPost ? '    └─ ' : '    ├─ ') : (isLastPost ? '│   └─ ' : '│   ├─ ');
                return (
                  <a href={`/blog/${post.slug}`} class="tree-item">
                    <span class="tree-prefix">{postPrefix}</span>
                    <span class="tree-item-title">{post.data.title}</span>
                    <time datetime={post.data.pubDate.toISOString()}>
                      {post.data.pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                    </time>
                  </a>
                );
              })}
            </div>
          </div>
        );
      })}
    </div>
  </main>

  <div class="bottom-bar">
    <a href="https://github.com/ButterHost69" target="_blank" rel="noopener noreferrer" aria-label="GitHub">
      <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" class="bottom-icon">
        <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" />
      </svg>
    </a>
    <a href="https://linkedin.com/in/palash-spatel" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">
      <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true" class="bottom-icon">
        <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
      </svg>
    </a>
  </div>
</BaseLayout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Iosevka+Charon:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap');

  .sidebar {
    position: fixed;
    left: 0;
    top: 0;
    width: 260px;
    height: 100vh;
    background: var(--violet);
    padding: var(--space-lg) var(--space-md);
    overflow-y: auto;
    z-index: 10;
  }

  .sidebar-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .sidebar-logo {
    font-family: var(--font-display);
    font-size: 1.25rem;
    color: var(--ivory);
    text-decoration: none;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .sidebar-logo:hover {
    color: var(--accent);
  }

  .separator {
    height: 2px;
    background: var(--ink);
    margin: var(--space-sm) 0;
    border-radius: 1px;
    opacity: 0.3;
  }

  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .sidebar-nav a {
    font-family: "Iosevka Charon", monospace;
    font-weight: 500;
    font-style: italic;
    font-size: 1.1rem;
    color: var(--ivory);
    text-decoration: none;
    transition: color 0.2s;
  }

  .sidebar-nav a:hover {
    color: var(--accent);
  }

  .main-content {
    margin-left: 260px;
    padding: var(--space-lg) var(--space-xl);
    min-height: 100vh;
    box-sizing: border-box;
    padding-bottom: calc(var(--space-lg) + 3rem);
  }

  .page-title {
    font-family: 'Iosevka Charon', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    color: var(--ink);
    margin-bottom: var(--space-lg);
  }

  .tree-container {
    font-family: monospace;
    font-size: 0.95rem;
    line-height: 1.8;
    color: var(--ink);
  }

  .tree-group {
    margin-bottom: var(--space-sm);
  }

  .tree-group-header {
    display: flex;
    align-items: baseline;
    gap: 0.2rem;
    color: var(--dark);
    font-weight: 600;
    font-size: 1rem;
  }

  .tree-group-name {
    color: var(--violet);
    font-weight: 700;
  }

  .tree-group-children {
    display: flex;
    flex-direction: column;
  }

  .tree-item {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    color: var(--ink);
    text-decoration: none;
    padding: 0.15rem 0;
    transition: color 0.2s;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .tree-item:hover {
    color: var(--violet);
  }

  .tree-item:hover .tree-item-title {
    text-decoration: underline;
    text-decoration-color: var(--accent);
    text-underline-offset: 3px;
  }

  .tree-prefix {
    font-family: monospace;
    color: var(--muted);
    white-space: pre;
    flex-shrink: 0;
  }

  .tree-item-title {
    flex: 1;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .tree-item time {
    font-size: 0.8rem;
    color: var(--muted);
    flex-shrink: 0;
    white-space: nowrap;
  }

  .bottom-bar {
    position: fixed;
    bottom: 0;
    left: 260px;
    right: 0;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-md);
    background: rgba(254, 254, 254, 0.85);
    backdrop-filter: blur(6px);
    border-top: 1px solid var(--border);
    z-index: 10;
  }

  .bottom-bar a {
    color: black;
    transition: color 0.2s;
    display: flex;
    align-items: center;
  }

  .bottom-bar a:hover {
    color: var(--violet);
  }

  .bottom-icon {
    width: 1.3rem;
    height: 1.3rem;
  }

  @media (max-width: 768px) {
    .sidebar {
      display: none;
    }

    .main-content {
      margin-left: 0;
      padding: var(--space-md);
      padding-bottom: calc(var(--space-md) + 3rem);
    }

    .bottom-bar {
      left: 0;
    }
  }
</style>
